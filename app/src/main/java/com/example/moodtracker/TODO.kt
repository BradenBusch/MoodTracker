package com.example.moodtracker

/**
 * Write anything that is a TODO in here
 */
class TODO {
    /*
     * Fix the SignUp screen so that it looks nicer, maybe add an image and center the stuff.
     *  - Change the button scheme. I think it should start white and maybe shrink when clicked.
     * Make a Login screen.
     * Write code to validate Signup and Login.
     * Pass the current users PIN in an Intent
     */
}