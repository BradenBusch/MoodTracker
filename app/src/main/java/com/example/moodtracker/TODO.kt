package com.example.moodtracker

/**
 * Write anything that is a TODO in here
 */
class TODO {
    /*
     * Fix the SignUp and Login screen so that it looks nicer, maybe add an image and center the stuff.
     *  - Change the button scheme.
     *  - Add a logo or "Welcome" or something like that idk. just not the form.
     * Write code to validate Signup and Login.
     * Pass the current users PIN in an Intent
     */
}